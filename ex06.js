let tab = [-2, 1, 4]

function subtract() {
  let x = parseInt(prompt("geef getal in"));
  let y = (tab[0]);
  if ((x - y) > 0) {
    document.write("Getal positief");
  } else {
    document.write("Negatief getal!");
  }
}
